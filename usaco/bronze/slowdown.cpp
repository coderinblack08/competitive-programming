#include <bits/stdc++.h>
#define ll long long
using namespace std;

void setIO(string s) {
  ios_base::sync_with_stdio(0); cin.tie(0); 
  freopen((s + ".in").c_str(), "r", stdin);
  freopen((s + ".out").c_str(), "w", stdout);
}

int main() {
  ll n, speed = 1;
  double currentDistance, currentTime;
  priority_queue<ll, vector<ll>, greater<ll>> timeEvents;
  priority_queue<ll, vector<ll>, greater<ll>> distanceEvents;
  cin >> n;
  
  return 0;
}